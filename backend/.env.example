# ============================================
# INFLUENCER MANAGER - BACKEND CONFIGURATION
# ============================================
# 
# SECURITY WARNING: Never commit the actual .env file to version control!
# This is an example file. Copy it to .env and fill in your actual values.
#
# For local development: cp .env.example .env
# ============================================

# --------------------------------------------
# Application Environment
# --------------------------------------------
# Options: development, production, test
NODE_ENV="development"

# --------------------------------------------
# Database Configuration
# --------------------------------------------
# Prisma supports: PostgreSQL, MySQL, SQLite, SQL Server, MongoDB, CockroachDB
# See: https://pris.ly/d/connection-strings
#
# For LOCAL DEVELOPMENT (SQLite):
# DATABASE_URL="file:./dev.db"
#
# For PRODUCTION (PostgreSQL):
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/influencer_manager?schema=public"

# --------------------------------------------
# Authentication & Security
# --------------------------------------------
# CRITICAL: Generate a strong random secret for production!
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# JWT token expiration time (examples: 7d, 24h, 60m, 30s)
JWT_EXPIRES_IN=7d

# --------------------------------------------
# Server Configuration
# --------------------------------------------
# Port the server will listen on
PORT=3000

# CORS allowed origins (comma-separated for multiple origins)
# For production, specify your frontend domain
# Example: CORS_ORIGIN="https://yourdomain.com,https://www.yourdomain.com"
CORS_ORIGIN="http://localhost:5173"
